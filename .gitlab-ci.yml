# Docker-py does not work in CI runners:
# https://gitlab.com/gitlab-org/gitlab-runner/-/issues/3442

stages:
  - "lint"
  - "test"

.build:
    before_script:
        - "pip --version"
        - "pip install virtualenv"
        - "python -m virtualenv /opt/venv"
        - "source /opt/venv/bin/activate; pip install docker"
        - "source /opt/venv/bin/activate; pip show docker"
        - "apt-get update -y"
        - "apt-get install apt-transport-https ca-certificates curl gnupg-agent software-properties-common -y"
        - "curl -fsSL https://download.docker.com/linux/debian/gpg | apt-key add -"
        - "add-apt-repository \"deb [arch=amd64] https://download.docker.com/linux/debian $(lsb_release -cs) stable\""
        - "apt-get update -y"
        - "apt-get install docker-ce docker-ce-cli containerd.io -y"
        - "service docker start"
        - "while systemctl is-active docker | grep \"inactive\" --q; do sleep 1; done"
        - "service docker status"
        - "docker info"
        - "source /opt/venv/bin/activate; python setup.py sdist"
        - "source /opt/venv/bin/activate; pip install --upgrade pip"
        - "source /opt/venv/bin/activate; pip install -r requirements-dev.txt"
        - "source /opt/venv/bin/activate; pip install docker>=2.5.1"
        - "source /opt/venv/bin/activate; pip install boto==2.45.0"
        - "source /opt/venv/bin/activate; pip install boto3"
        - "source /opt/venv/bin/activate; pip install dist/moto*.gz"
        - "source /opt/venv/bin/activate; pip install coveralls==1.1"
        - "source /opt/venv/bin/activate; pip install coverage==4.5.4"

lint:
  extends: .build
  stage: lint
  image: "python:3.7"
  script:
    - make lint

test-2.7:
  stage: test
  extends: .build
  image: "python:2.7"
  script:
    - make athena-test

test-3.6:
  stage: test
  extends: .build
  image: "python:3.6-buster"
  script:
    - make lambda-test

test-3.7:
  stage: test
  extends: .build
  image: "python:3.7"
  script:
    - make mbc-test