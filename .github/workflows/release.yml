name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version'
        required: true
        default: ''

jobs:
  release-moto-job:
    runs-on: ubuntu-latest
    name: Release Moto
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - name: Release Moto
      uses: ./.github/actions/moto-release/
      with:
        version: '${{ github.event.inputs.version }}'
